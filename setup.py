from setuptools import setup

setup(name='cassandra',
      version='0.1',
      description='The Drop Bears\' FRC prediction engine',
      url='https://github.com/thedropbears/cassandra',
      author='The Drop Bears',
      license='MIT',
      packages=['cassandra'],
      install_requires=[
      ],
      zip_safe=False)
